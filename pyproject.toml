[project]
name = "lopopolo-dotfiles"
version = "0.1.0"
description = "Personal dotfiles and bootstrap tooling."
readme = "README.md"
requires-python = ">=3.12"
classifiers = [
    "Private :: Do Not Upload",
]
dependencies = []

[dependency-groups]
dev = [
    "yamllint==1.38.0",
]

[tool.setuptools]
packages = []

[tool.uv]
package = false
